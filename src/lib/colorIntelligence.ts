// Comprehensive color name to HEX mapping with variations and shades
export const COLOR_NAME_MAP: Record<string, string> = {
  // Basic colors
  red: "#FF0000",
  orange: "#FF8C00",
  yellow: "#FFD700",
  green: "#228B22",
  blue: "#0066CC",
  purple: "#8B008B",
  pink: "#FF69B4",
  brown: "#8B4513",
  black: "#1A1A1A",
  white: "#FFFFFF",
  gray: "#808080",
  grey: "#808080",
  
  // Reds
  "dark red": "#8B0000",
  "darkred": "#8B0000",
  "light red": "#FF6B6B",
  "lightred": "#FF6B6B",
  crimson: "#DC143C",
  maroon: "#800000",
  scarlet: "#FF2400",
  ruby: "#E0115F",
  cherry: "#DE3163",
  brick: "#CB4154",
  "brick red": "#CB4154",
  wine: "#722F37",
  burgundy: "#800020",
  vermillion: "#E34234",
  cardinal: "#C41E3A",
  blood: "#8B0000",
  "blood red": "#8B0000",
  
  // Oranges
  "dark orange": "#FF8C00",
  "darkorange": "#FF8C00",
  "light orange": "#FFB366",
  "lightorange": "#FFB366",
  tangerine: "#FF9966",
  coral: "#FF7F50",
  peach: "#FFCBA4",
  apricot: "#FBCEB1",
  salmon: "#FA8072",
  "burnt orange": "#CC5500",
  rust: "#B7410E",
  copper: "#B87333",
  amber: "#FFBF00",
  pumpkin: "#FF7518",
  carrot: "#ED9121",
  
  // Yellows
  "dark yellow": "#DAA520",
  "darkyellow": "#DAA520",
  "light yellow": "#FFFFE0",
  "lightyellow": "#FFFFE0",
  gold: "#FFD700",
  golden: "#FFD700",
  mustard: "#FFDB58",
  lemon: "#FFF44F",
  canary: "#FFEF00",
  sunflower: "#FFDA03",
  honey: "#EB9605",
  cream: "#FFFDD0",
  ivory: "#FFFFF0",
  champagne: "#F7E7CE",
  banana: "#FFE135",
  
  // Greens
  "dark green": "#006400",
  "darkgreen": "#006400",
  "light green": "#90EE90",
  "lightgreen": "#90EE90",
  lime: "#32CD32",
  forest: "#228B22",
  "forest green": "#228B22",
  olive: "#808000",
  emerald: "#50C878",
  jade: "#00A86B",
  mint: "#98FF98",
  sage: "#9DC183",
  teal: "#008080",
  seafoam: "#71EEB8",
  chartreuse: "#7FFF00",
  grass: "#7CFC00",
  kelly: "#4CBB17",
  "kelly green": "#4CBB17",
  hunter: "#355E3B",
  "hunter green": "#355E3B",
  army: "#4B5320",
  "army green": "#4B5320",
  avocado: "#568203",
  pistachio: "#93C572",
  neon: "#39FF14",
  "neon green": "#39FF14",
  
  // Blues
  "dark blue": "#00008B",
  "darkblue": "#00008B",
  "light blue": "#ADD8E6",
  "lightblue": "#ADD8E6",
  navy: "#000080",
  "navy blue": "#000080",
  royal: "#4169E1",
  "royal blue": "#4169E1",
  sky: "#87CEEB",
  "sky blue": "#87CEEB",
  baby: "#89CFF0",
  "baby blue": "#89CFF0",
  cyan: "#00FFFF",
  turquoise: "#40E0D0",
  aqua: "#00FFFF",
  azure: "#007FFF",
  cobalt: "#0047AB",
  sapphire: "#0F52BA",
  ocean: "#006994",
  "ocean blue": "#006994",
  steel: "#4682B4",
  "steel blue": "#4682B4",
  powder: "#B0E0E6",
  "powder blue": "#B0E0E6",
  electric: "#7DF9FF",
  "electric blue": "#7DF9FF",
  midnight: "#191970",
  "midnight blue": "#191970",
  denim: "#1560BD",
  indigo: "#4B0082",
  prussian: "#003153",
  "prussian blue": "#003153",
  
  // Purples
  "dark purple": "#301934",
  "darkpurple": "#301934",
  "light purple": "#E6E6FA",
  "lightpurple": "#E6E6FA",
  violet: "#8B00FF",
  lavender: "#E6E6FA",
  magenta: "#FF00FF",
  fuchsia: "#FF00FF",
  plum: "#DDA0DD",
  mauve: "#E0B0FF",
  lilac: "#C8A2C8",
  orchid: "#DA70D6",
  grape: "#6F2DA8",
  amethyst: "#9966CC",
  eggplant: "#614051",
  aubergine: "#614051",
  mulberry: "#C54B8C",
  
  // Pinks
  "dark pink": "#E75480",
  "darkpink": "#E75480",
  "light pink": "#FFB6C1",
  "lightpink": "#FFB6C1",
  "hot pink": "#FF69B4",
  hotpink: "#FF69B4",
  rose: "#FF007F",
  blush: "#DE5D83",
  flamingo: "#FC8EAC",
  bubblegum: "#FFC1CC",
  dusty: "#D4A5A5",
  "dusty pink": "#D4A5A5",
  raspberry: "#E30B5C",
  
  // Browns
  "dark brown": "#5C4033",
  "darkbrown": "#5C4033",
  "light brown": "#C4A484",
  "lightbrown": "#C4A484",
  tan: "#D2B48C",
  beige: "#F5F5DC",
  chocolate: "#7B3F00",
  coffee: "#6F4E37",
  mocha: "#967969",
  caramel: "#FFD59A",
  chestnut: "#954535",
  cinnamon: "#D2691E",
  mahogany: "#C04000",
  walnut: "#773F1A",
  espresso: "#3C2415",
  sienna: "#A0522D",
  bronze: "#CD7F32",
  khaki: "#C3B091",
  taupe: "#483C32",
  umber: "#635147",
  sepia: "#704214",
  sand: "#C2B280",
  camel: "#C19A6B",
  
  // Grays
  "dark gray": "#404040",
  "darkgray": "#404040",
  "dark grey": "#404040",
  "darkgrey": "#404040",
  "light gray": "#D3D3D3",
  "lightgray": "#D3D3D3",
  "light grey": "#D3D3D3",
  "lightgrey": "#D3D3D3",
  charcoal: "#36454F",
  slate: "#708090",
  ash: "#B2BEB5",
  silver: "#C0C0C0",
  pewter: "#8BA8B7",
  graphite: "#383838",
  gunmetal: "#2A3439",
  platinum: "#E5E4E2",
  
  // Neutrals & Special
  clear: "#FFFFFF",
  transparent: "#FFFFFF",
  natural: "#F5F5DC",
  bone: "#E3DAC9",
  pearl: "#FDEEF4",
  snow: "#FFFAFA",
  "off white": "#FAF9F6",
  offwhite: "#FAF9F6",
  eggshell: "#F0EAD6",
  
  // Metallics
  metallic: "#AAA9AD",
  chrome: "#DBE4EB",
  brass: "#B5A642",
  
  // Specialty 3D printing colors
  wood: "#DEB887",
  marble: "#F0EAD6",
  galaxy: "#2E2B5F",
  rainbow: "#FF0000",
  multicolor: "#FF0000",
};

// Common color synonyms and variations
const COLOR_SYNONYMS: Record<string, string[]> = {
  red: ["scarlet", "crimson", "ruby", "cherry", "cardinal"],
  orange: ["tangerine", "coral", "peach", "apricot", "pumpkin"],
  yellow: ["gold", "golden", "lemon", "mustard", "canary"],
  green: ["lime", "forest", "olive", "emerald", "jade", "mint", "sage", "teal"],
  blue: ["navy", "royal", "sky", "cyan", "turquoise", "aqua", "azure", "cobalt"],
  purple: ["violet", "lavender", "magenta", "plum", "mauve", "lilac", "grape"],
  pink: ["rose", "blush", "salmon", "flamingo", "raspberry"],
  brown: ["tan", "beige", "chocolate", "coffee", "mocha", "caramel"],
  gray: ["charcoal", "slate", "ash", "silver", "graphite"],
  grey: ["charcoal", "slate", "ash", "silver", "graphite"],
};

/**
 * Check if a string is a color name (or contains a color name)
 */
export function isColorName(text: string): boolean {
  if (!text || text.length < 2) return false;
  const normalized = text.toLowerCase().trim();
  return normalized in COLOR_NAME_MAP;
}

// Terms that should NOT be treated as color searches even if they match color names
// These are common 3D printing terms that happen to match color names
const NON_COLOR_SEARCH_TERMS = new Set([
  // Materials that match color names
  "gold", "golden", "silver", "bronze", "copper", "brass", "chrome", "platinum",
  "wood", "marble", "natural", "clear", "transparent",
  // Brand/product terms that could match
  "silk", "matte", "galaxy", "rainbow", "multicolor", "neon",
  // Common material abbreviations - ensure these don't get matched
  "pla", "abs", "petg", "asa", "tpu", "pc", "pa", "cf", "gf",
]);

// Words that indicate the search is NOT a pure color search
// (i.e., user is searching for products, not filtering by color)
const PRODUCT_SEARCH_INDICATORS = new Set([
  // Materials
  "pla", "abs", "petg", "asa", "tpu", "pc", "pa", "nylon", "carbon", "fiber", "filament",
  // Brands (common ones)
  "bambu", "polymaker", "prusament", "esun", "hatchbox", "overture", "sunlu", "elegoo",
  "inland", "amazon", "eryone", "duramic", "gst3d", "amolen", "mika3d", "creality",
  // Product terms
  "basic", "pro", "plus", "lite", "matte", "silk", "glitter", "sparkle", "glow",
  "high", "speed", "tough", "flex", "soft", "hard", "strong",
]);

/**
 * Extract color name from text if present
 * Returns the color name and its hex code, or null if not found
 * 
 * IMPORTANT: This function is intentionally conservative to avoid
 * treating product searches as color searches. For example:
 * - "bambu pla" should NOT match (contains brand/material)
 * - "red" should match (pure color search)
 * - "red pla" should NOT match (color + material = product search)
 */
export function extractColorFromText(text: string): { colorName: string; hex: string } | null {
  if (!text || text.length < 2) return null;
  const normalized = text.toLowerCase().trim();
  const words = normalized.split(/\s+/);
  
  // If any word indicates a product search, don't treat as color search
  // This prevents "bambu pla", "red pla", "gold silk" from being color-only searches
  if (words.some(word => PRODUCT_SEARCH_INDICATORS.has(word))) {
    return null;
  }
  
  // If the term is in the non-color search terms, skip color extraction
  if (words.some(word => NON_COLOR_SEARCH_TERMS.has(word))) {
    return null;
  }
  
  // Direct match first - only if single word or pure color phrase
  if (normalized in COLOR_NAME_MAP && words.length === 1) {
    return { colorName: normalized, hex: COLOR_NAME_MAP[normalized] };
  }
  
  // Check for compound colors (e.g., "light blue", "dark red")
  if (words.length === 2) {
    // Try joining first two words
    const compound = words.slice(0, 2).join(" ");
    if (compound in COLOR_NAME_MAP) {
      return { colorName: compound, hex: COLOR_NAME_MAP[compound] };
    }
    // Try without space
    const compoundNoSpace = words.slice(0, 2).join("");
    if (compoundNoSpace in COLOR_NAME_MAP) {
      return { colorName: compoundNoSpace, hex: COLOR_NAME_MAP[compoundNoSpace] };
    }
  }
  
  // For single-word searches, check if it's a color
  if (words.length === 1 && words[0] in COLOR_NAME_MAP) {
    return { colorName: words[0], hex: COLOR_NAME_MAP[words[0]] };
  }
  
  // Don't extract colors from multi-word searches unless it's a compound color
  // This prevents "bambu red" from being treated as a color-only search
  return null;
}

/**
 * Get hex code for a color name
 */
export function colorNameToHex(colorName: string): string | null {
  const result = extractColorFromText(colorName);
  return result?.hex || null;
}

/**
 * Get suggested color searches based on partial input
 */
export function getColorSuggestions(partial: string, limit: number = 8): Array<{ name: string; hex: string }> {
  if (!partial || partial.length < 2) return [];
  
  const normalized = partial.toLowerCase().trim();
  const suggestions: Array<{ name: string; hex: string }> = [];
  const seen = new Set<string>();
  
  // Prioritize exact prefix matches
  for (const [name, hex] of Object.entries(COLOR_NAME_MAP)) {
    if (name.startsWith(normalized) && !seen.has(hex)) {
      suggestions.push({ name, hex });
      seen.add(hex);
    }
    if (suggestions.length >= limit) break;
  }
  
  // Then include contains matches
  if (suggestions.length < limit) {
    for (const [name, hex] of Object.entries(COLOR_NAME_MAP)) {
      if (name.includes(normalized) && !seen.has(hex)) {
        suggestions.push({ name, hex });
        seen.add(hex);
      }
      if (suggestions.length >= limit) break;
    }
  }
  
  return suggestions;
}

/**
 * Get the base color family for a given hex code
 */
export function getColorFamily(hex: string): string {
  const rgb = hexToRgb(hex);
  if (!rgb) return "unknown";
  
  const { r, g, b } = rgb;
  const max = Math.max(r, g, b);
  const min = Math.min(r, g, b);
  const lightness = (max + min) / 2 / 255;
  const saturation = max === min ? 0 : (max - min) / (1 - Math.abs(2 * lightness - 1)) / 255;
  
  // Check for neutrals first
  if (saturation < 0.1) {
    if (lightness > 0.9) return "white";
    if (lightness < 0.15) return "black";
    return "gray";
  }
  
  // Calculate hue
  let hue = 0;
  const d = max - min;
  if (max === r) {
    hue = ((g - b) / d + (g < b ? 6 : 0)) * 60;
  } else if (max === g) {
    hue = ((b - r) / d + 2) * 60;
  } else {
    hue = ((r - g) / d + 4) * 60;
  }
  
  // Map hue to color family
  if (hue < 15 || hue >= 345) return "red";
  if (hue < 45) return "orange";
  if (hue < 75) return "yellow";
  if (hue < 165) return "green";
  if (hue < 195) return "cyan";
  if (hue < 255) return "blue";
  if (hue < 285) return "purple";
  if (hue < 345) return "pink";
  
  return "unknown";
}

export function hexToRgb(hex: string): { r: number; g: number; b: number } | null {
  const result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
  return result
    ? {
        r: parseInt(result[1], 16),
        g: parseInt(result[2], 16),
        b: parseInt(result[3], 16),
      }
    : null;
}

/**
 * Default hex values for each color family
 */
export const COLOR_FAMILY_DEFAULTS: Record<string, string> = {
  white: "#FFFFFF",
  black: "#1A1A1A",
  red: "#FF0000",
  orange: "#FF8C00",
  yellow: "#FFD700",
  green: "#228B22",
  blue: "#0066CC",
  purple: "#8B008B",
  pink: "#FF69B4",
  brown: "#8B4513",
  gray: "#808080",
  grey: "#808080",
  gold: "#FFD700",
  silver: "#C0C0C0",
  copper: "#B87333",
  bronze: "#CD7F32",
  beige: "#F5F5DC",
  clear: "#FFFFFF",
  natural: "#F5F5DC",
  transparent: "#FFFFFF",
  teal: "#008080",
  cyan: "#00FFFF",
  navy: "#000080",
  maroon: "#800000",
  olive: "#808000",
};

/**
 * Get the default hex code for a color family
 */
export function getHexForFamily(colorFamily: string): string | null {
  if (!colorFamily) return null;
  const normalized = colorFamily.toLowerCase().trim();
  return COLOR_FAMILY_DEFAULTS[normalized] || null;
}

/**
 * Check if a hex code is valid for a given color family
 * Returns true if the hex appears to match the expected family
 */
export function isHexValidForFamily(hex: string, colorFamily: string): boolean {
  if (!hex || !colorFamily) return false;
  
  const rgb = hexToRgb(hex);
  if (!rgb) return false;
  
  const { r, g, b } = rgb;
  const normalizedFamily = colorFamily.toLowerCase().trim();
  
  // Check for obvious mismatches
  const brightness = (r + g + b) / 3;
  
  // White should be bright
  if (normalizedFamily === "white" && brightness < 200) return false;
  
  // Black should be dark
  if (normalizedFamily === "black" && brightness > 80) return false;
  
  // Check saturation for colored families
  const max = Math.max(r, g, b);
  const min = Math.min(r, g, b);
  const saturation = max === 0 ? 0 : (max - min) / max;
  
  // Colored families should have some saturation (unless very light/dark)
  const coloredFamilies = ["red", "orange", "yellow", "green", "blue", "purple", "pink", "cyan", "teal"];
  if (coloredFamilies.includes(normalizedFamily) && saturation < 0.1 && brightness > 50 && brightness < 200) {
    return false;
  }
  
  return true;
}

/**
 * Validate and potentially fix a color hex based on the color family
 * Returns the original hex if valid, or a corrected hex if mismatched
 */
export function validateColorHex(hex: string | null, colorFamily: string | null): string | null {
  // If no family, return the hex as-is
  if (!colorFamily) return hex;
  
  // If no hex but we have a family, derive it
  if (!hex) {
    return getHexForFamily(colorFamily);
  }
  
  // Validate the hex against the family
  if (!isHexValidForFamily(hex, colorFamily)) {
    // The hex doesn't match the family - return the default for that family
    return getHexForFamily(colorFamily) || hex;
  }
  
  return hex;
}

/**
 * Extract color information from a product title
 * Returns hex and family if a color is detected
 */
export function extractColorFromProductTitle(title: string): { hex: string; family: string } | null {
  if (!title) return null;
  
  const normalized = title.toLowerCase();
  
  // Check for color names in the title (prioritize longer matches)
  const sortedColors = Object.entries(COLOR_NAME_MAP)
    .sort((a, b) => b[0].length - a[0].length);
  
  for (const [colorName, hex] of sortedColors) {
    // Use word boundary matching to avoid partial matches
    const regex = new RegExp(`\\b${colorName.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')}\\b`, 'i');
    if (regex.test(normalized)) {
      const family = getColorFamily(hex);
      return { hex, family };
    }
  }
  
  return null;
}
